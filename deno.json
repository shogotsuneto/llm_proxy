{
  "tasks": {
    "dev": "deno run --watch main.ts",
    "build": "deno compile --allow-net --output=bin/llm_proxy main.ts",
    "podman-build": "podman build -t llm_proxy ."
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1"
  }
}
